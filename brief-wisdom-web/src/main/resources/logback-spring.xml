<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">

    <!-- 属性定义 -->
    <property name="LOG_HOME" value="logs"/>
    <property name="APP_NAME" value="brief-wisdom"/>
    <!-- MDC 结构化日志：包含 userId 和 sessionId 上下文 -->
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{userId:-}] [%X{sessionId:-}] %-5level %logger{36} - %msg%n"/>
    <property name="CONSOLE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{userId:-}] [%X{sessionId:-}] %highlight(%-5level) %cyan(%logger{36}) - %msg%n"/>

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- INFO 级别日志文件 -->
    <appender name="INFO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${APP_NAME}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <!-- 滚动策略：按天分割，单个文件最大100MB -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${APP_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
        <!-- 只记录 INFO 及以上级别 -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
    </appender>

    <!-- ERROR 级别日志文件（单独记录错误） -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${APP_NAME}-error.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${APP_NAME}-error-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>90</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
        <!-- 只记录 ERROR 级别 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- AI 相关日志单独文件 -->
    <appender name="AI_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${APP_NAME}-ai.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${APP_NAME}-ai-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <!-- 简历相关日志单独文件 -->
    <appender name="RESUME_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${APP_NAME}-resume.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${APP_NAME}-resume-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <!-- Logger 配置 -->
    
    <!-- AI 模块日志（开发环境 DEBUG，生产环境 INFO） -->
    <logger name="com.mouhin.brief.wisdom.ai" additivity="false">
        <appender-ref ref="AI_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- 简历模块日志（开发环境 DEBUG，生产环境 INFO） -->
    <logger name="com.mouhin.brief.wisdom.resume" additivity="false">
        <appender-ref ref="RESUME_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Spring Framework 日志级别调整 -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.security" level="WARN"/>
    
    <!-- MyBatis SQL 日志（开发环境 DEBUG，生产环境 WARN） -->
    <logger name="com.mouhin.brief.wisdom.persistence.mapper" level="WARN"/>
    
    <!-- 抑制 Chrome DevTools 探测请求的无资源告警 -->
    <logger name="org.springframework.web.servlet.resource.NoResourceFoundException" level="ERROR"/>
    <logger name="org.springframework.web.servlet.resource.ResourceHttpRequestHandler" level="ERROR"/>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="INFO_FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

    <!-- 开发环境：开启 DEBUG 日志 -->
    <springProfile name="dev">
        <logger name="com.mouhin.brief.wisdom.ai" level="DEBUG"/>
        <logger name="com.mouhin.brief.wisdom.resume" level="DEBUG"/>
        <logger name="com.mouhin.brief.wisdom.persistence.mapper" level="DEBUG"/>
    </springProfile>

    <!-- 生产环境：仅 INFO/WARN/ERROR -->
    <springProfile name="prod">
        <logger name="com.mouhin.brief.wisdom.ai" level="INFO"/>
        <logger name="com.mouhin.brief.wisdom.resume" level="INFO"/>
        <logger name="com.mouhin.brief.wisdom.persistence.mapper" level="WARN"/>
    </springProfile>

</configuration>
