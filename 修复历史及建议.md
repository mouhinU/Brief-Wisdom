一、已实施的修复（6项）
1. 短信验证码安全加固 — SmsServiceImpl.java

java.util.Random 替换为 java.security.SecureRandom，防止验证码被预测。日志中验证码仅输出前2位（code=13****），避免日志泄露完整验证码。

2. API 响应不再返回验证码明文 — AuthController.java

sendSmsCode 接口的响应中移除了 "code", code 字段。此前攻击者可以直接从 HTTP 响应中读取验证码，完全绕过短信验证。改为仅在 debug 日志中记录（带手机号脱敏）。

3. OAuth 登录 Session key 修复 — OauthCallbackController.java

原来 OAuth 回调使用 "AUTH_USER" 作为 session key，而 UserContextHelper 和其他所有 Controller 使用 "SESSION_USER"。这导致钉钉/支付宝登录后，getCurrentUserId() 返回 null，权限校验全部失效。修复为引用 UserContextHelper.SESSION_USER_KEY 常量。

4. 手机号自动注册用户名冲突 — AuthServiceImpl.java

原来用手机号后4位生成用户名（user_1234），不同手机号后4位相同时会冲突。改为 user_{手机号}_{UUID前6位}，确保唯一性。

5. 配置安全加固 — application.yml + application-dev.yml

DashScope / DeepSeek API 密钥从硬编码明文改为 ${DASHSCOPE_API_KEY} 环境变量
数据库密码改为 ${DB_PASSWORD}，Redis 密码改为 ${REDIS_PASSWORD}
DevTools、SQL 日志等开发配置从 base config 移至 application-dev.yml
profiles.include: dev 改为 profiles.active: ${SPRING_PROFILES_ACTIVE:dev}，生产环境设置 SPRING_PROFILES_ACTIVE=prod 即可
6. 前端 XSS 防护 — chat.js + navbar.js

引入 DOMPurify 库，所有 marked.parse() 输出经过 DOMPurify.sanitize() 过滤后再渲染到 DOM，防止 AI 回复中的恶意脚本执行。新增 renderMarkdown() 统一渲染函数，5 处 innerHTML 赋值全部替换。

二、待处理的建设性建议（按优先级排列）
紧急（建议本周内处理）— ✅ 已于 2026-07-08 完成
CORS 配置过度开放：已在 SecurityConfig 中统一配置，新增 application-prod.yml 与 CORS_ALLOWED_ORIGINS 环境变量。

知识库接口缺少权限控制：已在 SecurityConfig 限制 /api/knowledge/** 为 admin/super_admin。

N+1 查询问题：AiManageServiceImpl 已改为批量聚合查询。

Markdown 路径穿越风险：已新增 SafePathUtils，import-allowed-dirs/files 白名单校验。

全局异常 HTTP 200：未知异常已改为 HTTP 500。

SSE 错误 JSON 拼接：AiAgentController 已改用 ObjectMapper 序列化。

Controller 直接接收持久化实体：ResumeManageController、AiModelController 已改用 Request DTO。

重要（建议本月内处理）— ✅ 已于 2026-07-08 完成

- **SQL 索引**：Flyway `V2__add_performance_indexes.sql` 补充 `chat_session.user_id`、`chat_message.session_id` 等性能索引
- **Docker 安全**：MySQL/Redis 绑定 `127.0.0.1`，增加 `deploy.resources.limits`，默认 `prod` profile
- **Flyway 迁移**：`V1__baseline.sql` + `V2__add_performance_indexes.sql`，版本化数据库变更
- **CI/CD**：`.github/workflows/ci.yml` 自动 compile + test
- **Actuator + Prometheus**：健康检查与 metrics 端点已接入
- **单元测试**：新增 `AuthServiceImplTest`（9 个用例）；70% 覆盖率目标仍待推进

改进（建议逐步推进）— ✅ 核心项已于 2026-07-08 完成

- **统一 apiRequest**：新增 `static/js/api-request.js`，管理端组件（用户/知识库/简历等）已迁移
- **OpenAPI 注解**：补全 `SseSyncController`、`WechatAuthController`、`OauthCallbackController`、`ResumeAiController`
- **前端 IDE 支持**：新增 `jsconfig.json`；完整 TypeScript 迁移仍待规划